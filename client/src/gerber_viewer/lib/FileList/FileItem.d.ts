/// <reference types="react" />
import { LayerRender } from '../types';
declare type Props = LayerRender & {
    showFilenames: boolean;
};
export default function FileItem(props: Props): JSX.Element;
export {};
