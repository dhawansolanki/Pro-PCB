/// <reference types="react" />
import { BoardRender } from '../types';
export declare type BoardSettingsProps = {
    board: BoardRender;
    updating: boolean;
};
export default function BoardSettings(props: BoardSettingsProps): JSX.Element;
