import React from 'react';
import cx from 'classnames';
import { Logo } from './Logo';
export function PageTitle(props) {
    const { subtitle, className } = props;
    return (
        // React.createElement("div", { className: cx('dib', className) },
        // React.createElement(Logo, { className: "pl1 mr3 v-btm brand" }),
        // React.createElement("h1", { className: "dib mv0 f3 lh-title normal" },
        //     "tracespace",
        //     subtitle && React.createElement("span", { className: "b" }, ` ${subtitle}`)))
        ""
            );
}
//# sourceMappingURL=PageTitle.js.map